'use client';import {useEffect} from 'react';import {useRouter} from 'next/navigation';import {hosted,supabase} from '@/lib/supabase';
export default function Home(){const router=useRouter();useEffect(()=>{if(!hosted){router.replace('/dashboard');return}supabase!.auth.getSession().then(({data})=>router.replace(data.session?'/dashboard':'/login'))},[router]);return <main className="auth-shell"><section className="auth-card"><h1>Job Hunter</h1><p>Preparing your workspace…</p></section></main>}
