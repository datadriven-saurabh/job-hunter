'use client';
import {useState} from 'react';
import {api} from '@/lib/api';
export default function ApplicationAnswers({job}:{job:any}){
 const [questions,setQuestions]=useState(''),[maxWords,setMaxWords]=useState('150'),[maxChars,setMaxChars]=useState(''),[answers,setAnswers]=useState<any[]>([]),[busy,setBusy]=useState(false),[error,setError]=useState('');
 return <details className="evidence-editor"><summary>Answer application questions</summary><p>One question per line. Behavioural answers use your verified story bank; missing stories are flagged for review.</p><label>Questions<textarea rows={5} value={questions} onChange={e=>setQuestions(e.target.value)} placeholder="Why are you interested in this role?"/></label><div className="form-grid"><label>Maximum words<input type="number" min="1" max="500" value={maxWords} onChange={e=>setMaxWords(e.target.value)}/></label><label>Maximum characters · optional<input type="number" min="1" max="10000" value={maxChars} onChange={e=>setMaxChars(e.target.value)}/></label></div><button className="primary" disabled={busy||!questions.trim()} onClick={async()=>{setBusy(true);setError('');try{const result=await api('/career/answers','POST',{target_jd:job,questions:questions.split('\n').filter(x=>x.trim()).map(question=>({question,max_words:maxWords?Number(maxWords):null,max_characters:maxChars?Number(maxChars):null,mode:'draft'}))});setAnswers(result.answers)}catch(e){setError(e instanceof Error?e.message:'Could not generate answers')}finally{setBusy(false)}}}>{busy?'Preparing evidence-based answers…':'Draft answers'}</button>{error&&<p role="alert">{error}</p>}{answers.map((a,i)=><section className="evidence-row" key={i}><h4>{a.data.question}</h4><small>{a.data.category} · {a.validation.word_count} words · {a.validation.character_count} characters{a.data.story_id&&` · Story: ${a.data.story_id}`}</small><textarea rows={6} readOnly value={a.answer||a.preview}/>{a.validation.errors.map((e:any)=><p key={e.code} className="validation-error">{e.message}</p>)}{a.status==='valid'&&<small>Verified source-based draft. Review before pasting into the employer form.</small>}</section>)}</details>
}
