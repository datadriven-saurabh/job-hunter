'use client';
import {useState} from 'react';import Link from 'next/link';import {useRouter,useSearchParams} from 'next/navigation';import {supabase} from '@/lib/supabase';
export default function AuthForm({mode}:{mode:'login'|'signup'}){const router=useRouter(),params=useSearchParams(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');if(!supabase){router.push('/dashboard');return}const result=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{emailRedirectTo:`${window.location.origin}/dashboard`}});setBusy(false);if(result.error){setError(result.error.message);return}if(result.data.session)router.replace(mode==='signup'?'/onboarding':'/dashboard');else setNotice('Check your email to confirm the account, then sign in.')}return <main className="auth-shell"><section className="auth-card"><div className="auth-brand">◎ <strong>Job Hunter</strong></div><p className="eyebrow">PRIVATE BETA</p><h1>{mode==='login'?'Welcome back':'Create your workspace'}</h1><p>{mode==='login'?'Sign in to your private job search workspace.':'Your resume, preferences, matches, and application drafts stay isolated from every other account.'}</p>{params.get('expired')&&<p className="error-banner">Your session expired. Sign in again.</p>}{error&&<p className="error-banner">{error}</p>}{notice&&<p className="success-banner">{notice}</p>}<form onSubmit={submit}><label>Email<input type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" minLength={8} required autoComplete={mode==='login'?'current-password':'new-password'} value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary full" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button></form><p>{mode==='login'?<>New to Job Hunter? <Link href="/signup">Create an account</Link></>:<>Already invited? <Link href="/login">Sign in</Link></>}</p></section></main>}
